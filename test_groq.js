const fs = require('fs'); const key = JSON.parse(fs.readFileSync('.env.local', 'utf8').split('\n').find(l => l.startsWith('GROQ_API_KEY=')).split('=')[1]); fetch('https://api.groq.com/openai/v1/chat/completions', { method: 'POST', headers: { 'Content-Type': 'application/json', 'Authorization': 'Bearer ' + key }, body: JSON.stringify({ model: 'llama-3.3-70b-versatile', messages: [{role: 'user', content: 'hello'}], max_tokens: 100 }) }).then(r => r.json()).then(console.log).catch(console.error);
