import dotenv from 'dotenv'; dotenv.config({path:'.env.local'}); import { generateWithFallback, generateFast } from './lib/ai-providers.js'; async function run() { try { const res = await generateWithFallback({ systemPrompt: 'Test', userPrompt: 'Hello' }); console.log('generateWithFallback SUCCESS:', res.provider); } catch (e) { console.error('generateWithFallback ERROR:', e.message); } try { const res2 = await generateFast({ systemPrompt: 'Test', userPrompt: 'Hello' }); console.log('generateFast SUCCESS:', res2.provider); } catch (e) { console.error('generateFast ERROR:', e.message); } } run();
