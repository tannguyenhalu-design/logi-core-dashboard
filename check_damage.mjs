import fs from 'fs'; import { google } from 'googleapis'; const key = JSON.parse(fs.readFileSync('.env.local', 'utf8').match(/GOOGLE_SERVICE_ACCOUNT_KEY='([^']+)'/)[1]); const auth = new google.auth.GoogleAuth({ credentials: key, scopes: ['https://www.googleapis.com/auth/spreadsheets'] }); const sheets = google.sheets({ version: 'v4', auth }); sheets.spreadsheets.values.get({ spreadsheetId: '1Nj1IMAOH_mdmvNImgS6KPelP9dXvPWF9aWZjEhM58Pc', range: 'raw_damage' }).then(res => { const rows = res.data.values; const headers = rows[0]; const data = rows.slice(1).map(r => { let obj = {}; headers.forEach((h, i) => obj[h] = r[i]); return obj; }); console.log('Total raw_damage rows:', data.length); const noDate = data.filter(r => !r['pickup_time'] && !r['case_date']); console.log('No date:', noDate.length); const noDM = data.filter(r => { const name = String(r['client_name']).toLowerCase(); return !['nguy?n kim', 'psd', 'samsung', 'aqua', 'lg ltl', 'lg pantos', 'casper', 'bluestone', 'elmich', 'toshiba', 'hisense', 'cellphones', '266', 'h?ng d?t'].some(kw => name.includes(kw)); }); console.log('Not recognized DM:', noDM.map(r => r.client_name)); }).catch(console.error);
